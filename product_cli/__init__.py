"""product_cli — the Product-Kit CLI: scaffold the toolkit into a project
(`product init`) and run a headless mission (`product run`) over the unchanged
`product_kit` engine."""

__version__ = "0.1.0"
